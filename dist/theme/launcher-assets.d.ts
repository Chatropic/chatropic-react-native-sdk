import type { ColorScheme, WidgetConfig } from "../types";
export declare function launcherButtonBackground(config: WidgetConfig, colorScheme: ColorScheme): string;
export declare function launcherButtonStyle(config: WidgetConfig, colorScheme: ColorScheme): {
    backgroundColor: string;
    color: string;
};
