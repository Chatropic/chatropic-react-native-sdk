import React from "react";
interface VoiceWaveformIconProps {
    active?: boolean;
    color?: string;
}
/** Chatbase-style live voice waveform button icon (matches SaaS composer). */
export declare function VoiceWaveformIcon({ active, color, }: VoiceWaveformIconProps): React.JSX.Element;
export {};
