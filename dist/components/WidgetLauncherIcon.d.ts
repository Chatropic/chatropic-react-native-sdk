import React from "react";
import type { ColorScheme } from "../types";
interface WidgetLauncherIconProps {
    colorScheme: ColorScheme;
    size?: number;
}
export declare function WidgetLauncherIcon({ colorScheme, size, }: WidgetLauncherIconProps): React.JSX.Element;
export {};
